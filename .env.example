TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here
# 私有化 TG 服务时填写，必须以 /bot 结尾，例如: https://your-api.com/bot
TELEGRAM_API_BASE_URL=

# ==================== 运行模式（只改这一项） ====================
# webhook   : 使用 webhook（推荐；对外平台主动回调你的服务）
# temporary : 临时模式（机器人走 polling，同时可开 hello 页面做公网连通验证）
RUNTIME_MODE=webhook

# ==================== webhook 配置（RUNTIME_MODE=webhook 时生效） ====================
# 公网 HTTPS 地址，例如：
# 1) 普通格式: https://your-domain.com
# 2) 你们平台要求格式: https://your-domain.com?bot_token=xxxxx
WEBHOOK_URL=
# Webhook 路径，如 /webhook 或留空
WEBHOOK_PATH=
# 本地监听地址与端口（ngrok 通常转发到这个端口）
WEBAPP_HOST=0.0.0.0
WEBAPP_PORT=8443
# 可选：请求验证 token（与平台设置 webhook 时的 secret_token 保持一致）
WEBHOOK_SECRET_TOKEN=

# ==================== hello 页面配置（联调用） ====================
# 说明：
# - RUNTIME_MODE=temporary 时，默认会尝试启动 hello 页面
# - RUNTIME_MODE=webhook 时，HELLO_WORLD_ENABLED=true 才会启动
HELLO_WORLD_ENABLED=false
HELLO_WORLD_PORT=8080
HELLO_WORLD_TEXT=hello world

# 日志与调试
DEBUG=False
# 日志文件路径（可选），设置后日志会写入该文件
# LOG_FILE=logs/bot.log
