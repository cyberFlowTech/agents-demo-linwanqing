# ==================== TG 平台切换（快速切换 Telegram / Zapry）====================
# telegram : 使用官方 Telegram API（标准小程序）
# zapry    : 使用 Zapry 私有化 TG 服务（私有化小程序）
# 只需修改这一项，下面的 Token 和 API 地址会自动切换
TG_PLATFORM=telegram

# ==================== Telegram 官方配置 ====================
TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here

# ==================== Zapry 私有化配置 ====================
ZAPRY_BOT_TOKEN=your_zapry_bot_token_here
# ⚠️ 注意：API Base URL 必须以 / 结尾，否则 API 调用会失败
ZAPRY_API_BASE_URL=https://openapi.mimo.immo/bot/

# ==================== 运行模式（只改这一项）====================
# webhook   : 使用 webhook（推荐；对外平台主动回调你的服务）
# temporary : 临时模式（机器人走 polling，同时可开 hello 页面做公网连通验证）
RUNTIME_MODE=webhook

# ==================== Webhook 配置（RUNTIME_MODE=webhook 时生效）====================
# Telegram 官方 Webhook 地址（使用 TG_PLATFORM=telegram 时生效）
TELEGRAM_WEBHOOK_URL=https://your-domain.com

# Zapry Webhook 地址（使用 TG_PLATFORM=zapry 时生效）
# Zapry 要求格式：https://域名?bot_token=你的token
ZAPRY_WEBHOOK_URL=https://your-ngrok-domain.ngrok-free.dev?bot_token=your_zapry_token

WEBHOOK_PATH=
WEBAPP_HOST=0.0.0.0
WEBAPP_PORT=8443
WEBHOOK_SECRET_TOKEN=

# ==================== hello 页面配置（联调用） ====================
# 说明：
# - RUNTIME_MODE=temporary 时，默认会尝试启动 hello 页面
# - RUNTIME_MODE=webhook 时，HELLO_WORLD_ENABLED=true 才会启动
HELLO_WORLD_ENABLED=false
HELLO_WORLD_PORT=8080
HELLO_WORLD_TEXT=hello world

# ==================== 数据库配置 ====================
# SQLite 数据库路径（可选，默认 data/elena.db）
# DATABASE_PATH=data/elena.db

# 日志与调试
DEBUG=False
# 日志文件路径（可选），设置后日志会写入该文件
# LOG_FILE=logs/bot.log

# ==================== OpenAI 配置 ====================
# 林晚晴的 AI 对话功能需要 OpenAI API

# OpenAI API Key（必填）
OPENAI_API_KEY=your_openai_api_key_here

# OpenAI Base URL（选填，如果使用国内中转服务，填写中转地址）
# 例如: https://api.openai-proxy.com/v1
OPENAI_BASE_URL=

# OpenAI 模型（默认 gpt-4o-mini，也可以使用 gpt-4、gpt-3.5-turbo 等）
OPENAI_MODEL=gpt-4o-mini

